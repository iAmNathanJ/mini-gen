'use strict';

let test = require('tape');
let MiniGen = require('../');

test('MiniGen is a thing', t => {
  t.ok(MiniGen, 'It exists!');
  t.end();
});

test('MiniGen', t => {
  t.equal();
  t.end();
});
